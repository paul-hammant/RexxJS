{
  "name": "rexxjs-jq-address",
  "version": "1.0.0",
  "description": "jq ADDRESS library for RexxJS using native jq binary (fast, requires system jq)",
  "main": "src/jq-address.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "install:core": "cd ../../../core && npm install",
    "pretest": "npm run install:core"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "keywords": [
    "rexxjs",
    "jq",
    "json",
    "query",
    "native"
  ],
  "author": "RexxJS Project",
  "license": "MIT",
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.js",
      "**/*.test.js",
      "**/*.spec.js"
    ]
  },
  "type": "commonjs"
}
